package io.github.poeticrainbow.goldentweaks.enums;

public enum Versions {
    ALPHA,
    BETA,
    RELEASE,
    MODERN;

    @Override
    public String toString() {
        return super.toString().toLowerCase();
    }
}
